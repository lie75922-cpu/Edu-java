# 本地研究练习目录验收

当前实现：真实Junyi元数据导出、Java只读查询接口、Vue目录页面。不是完整题库、答题闭环或认知诊断系统。

## 实际验证

- 主会话核对后端Surefire：8项测试，失败/错误/跳过均0。
- 主会话重跑目录导出测试：4项通过。
- 浏览器打开 `http://127.0.0.1:5173/`，确认登录、搜索、筛选及分页控件正常渲染。
- 使用临时本地Basic凭据通过前端 `/api` 代理请求真实Java服务：默认总数837、首页20项首记录1；第二页20项首记录21。
- 搜索 `matrix_mul_two` 返回3项（子串搜索）；领域 `algebra` 返回258项；不存在的搜索词返回0项。
- 无认证请求返回401；size=101返回400。
- 浏览器填写凭据后的点击流程尚未验证；生产构建尚未验证。

## 本地运行

- 前端开发地址：`http://127.0.0.1:5173/`，后端仅监听 `127.0.0.1:8080`。
- 真实目录文件：`D:/Code/java/data-pipeline/data/processed/junyi_catalog_v1.json`。
- 后端脚本：`backend/scripts/start-research-catalog-local.ps1`，使用已编译classes及测试报告中已有依赖，不联网解析。
- 重启后端前，在启动shell设置 `EDU_RESEARCH_BASIC_PASSWORD`；默认用户名 `research-local`。密码不写入本文件。
- 当前只读本地启动绕过数据库相关配置，不代表MySQL、Redis、Neo4j或生产服务集成通过；启动日志仍显示Neo4j驱动创建，不能声称已完全禁用所有图数据库自动配置。
- 开发源码来自main源码包；本轮在独立Git检出中集成，交付分支为 `codex/junyi-catalog-20260907`。

## 已知限制

- 保留837行和835个外部名称，4行标记重复ID；未擅自选择合并策略。
- Maven在线解析曾产生校验类副文件，未满足用户永久限制；当前暂停该解析流程。
- 本次临时服务用于开发联调，未配置生产账户或部署。
