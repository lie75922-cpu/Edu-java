# 本轮提交与交付汇报

## 交付内容

1. Python CLI读取真实Junyi Exercise元数据，原子写入版本1目录JSON，保留重复名称和来源字段。
2. Java研究练习目录API支持认证、分页、名称搜索、领域和主题精确筛选，并明确报告数据文件/格式及参数错误。
3. Vue目录页面支持连接、搜索、筛选、分页、加载/空结果/失败状态，凭据不持久化。
4. 本地只读启动脚本利用已编译Java产物及已有依赖，不修改生产默认基础设施配置。

## 实际结果

- 真实导出837项、835个不同名称，4项标记重复外部ID。
- 后端8项测试通过，目录导出4项测试通过。
- 实际启动后通过前端代理读取目录：首页20项，下一页从记录21开始；algebra筛选258项。
- 搜索、空结果、未认证401和非法参数400均已验证。
- 浏览器页面正常渲染；浏览器登录查询点击流程和最终生产构建未验证。

## 提交边界

- 提交范围限定源码、必要测试、启动脚本、忽略规则及说明文档。
- 原始RAR/CSV、导出JSON、node_modules、target、工具依赖、日志和环境文件均不在提交范围。
- 本轮交付分支为 `codex/junyi-catalog-20260907`，从远程main建立，不直接修改main。
- 用户已明确允许Git自身管理提交对象；继续禁止额外生成、校验或汇报数据内容摘要。

## 未完成范围

- 本次是研究练习目录功能，不是完整的题库、答题、认知诊断和推荐系统。
- 数据库/图数据库集成和完整容器启动未验收。
- Maven在线解析曾生成校验副文件，已停止该流程；不宣称本轮所有工具执行均满足永久限制。
